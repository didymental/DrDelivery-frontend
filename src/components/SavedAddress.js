// to fetch from backend server API

const addressConfig = {
    name: 'Home',
    address: '14 Somewhere Road',
    postalCode: '123456'
}

const SavedAddress = () => {
    return addressConfig;
}

export default SavedAddress;